{
  "name": "atlas-blueprint-lab", "version": "0.1.0", "private": true, "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5185",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5185",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:golden": "playwright test --grep golden",
    "sources": "node scripts/acquire-plans.mjs",
    "reports": "node scripts/report.mjs",
    "checkpoint": "node scripts/checkpoint.mjs",
    "checkpoint:watch": "node scripts/checkpoint.mjs --watch"
  },
  "dependencies": { "three": "0.180.0" },
  "devDependencies": { "vite": "7.3.6", "vitest": "4.1.11", "@vitest/coverage-v8": "4.1.11", "@playwright/test": "1.58.2" }
}
