import {readFile,mkdir,writeFile,cp} from 'node:fs/promises';
const root=new URL('../',import.meta.url),out=new URL('public/reports/mobility/',root);
const json=async file=>JSON.parse(await readFile(new URL(file,root),'utf8'));
const [unit,coverage,e2e,catalog]=await Promise.all(['reports/mobility/unit/results.json','reports/mobility/coverage/coverage-summary.json','reports/mobility/e2e/results.json','public/mobility/catalog.json'].map(json));
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
await mkdir(out,{recursive:true});
for(const name of ['coverage','e2e'])await cp(new URL(`reports/mobility/${name}/`,root),new URL(name+'/',out),{recursive:true});
const passed=unit.success&&e2e.stats.expected===4&&e2e.stats.unexpected===0&&e2e.stats.skipped===0&&e2e.config.updateSnapshots==='none';
await writeFile(new URL('index.html',out),`<!doctype html><html lang="ko"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>실차·충전 데이터 검증 — ATLAS</title><style>body{margin:0;background:#f4f5ef;color:#304b3b;font:14px system-ui;line-height:1.8}main{max-width:1000px;margin:auto;padding:50px 5%}h1{font-size:34px;letter-spacing:-1px}h2{font-size:20px;margin-top:35px}a{color:#426d4f}section{padding:22px;border:1px solid #cfddc5;border-radius:10px;background:#fbfcf7;margin:18px 0}.stats{display:flex;gap:20px;flex-wrap:wrap}.stats>div{flex:1;min-width:180px}.stats b{font-size:32px;display:block;font-weight:500}table{width:100%;border-collapse:collapse;font-size:12px}td,th{text-align:left;padding:12px;border-bottom:1px solid #dce5d5}small{color:#8b9c7f}.status{padding:15px;background:${passed?'#e1edd9':'#f2e3cb'};border-radius:8px}</style><main><a href="/mobility.html">← 실차 경로·충전 데이터</a><h1>새 카테고리의 근거와 검증.</h1><p class="status">${passed?'기능·골든 독립 비교 통과':'검증 진행 중 또는 기준 갱신 실행 — 독립 비교 결과 확인 필요'}</p><div class="stats"><div><b>${unit.numPassedTests}</b>단위 테스트 통과</div><div><b>${coverage.total.lines.pct}%</b>새 핵심 모듈 줄 커버리지</div><div><b>${e2e.stats.expected} / 4</b>브라우저 기능·골든 통과</div><div><b>${catalog.sources.length}</b>출처별 자료</div></div><section><a href="coverage/index.html">코드 커버리지 HTML ↗</a> · <a href="e2e/index.html">브라우저·골든 상세 ↗</a><p>커버리지 대상은 <code>src/core/mobility.js</code>의 검색·차량 외곽 치수·차폭 기준 경로 후보 계산입니다. 기존 ATLAS 전체 SDK 커버리지와 구분합니다. 브라우저 검증은 데스크톱·모바일·출처 상세 골든, 검색·분류·내려받기, 경로 차단·재생·정지와 실제 B2 충전 화면 연결을 포함합니다.</p></section><h2>자료 활용의 범위</h2><section><p>동북권 B2 도면은 실제 공개 설계 자료이며 차로는 수동 주석, 축척은 추정값입니다. 차폭 조건만 비교하며 회전 궤적과 충돌을 검증한 실차 주행 경로가 아닙니다. Palo Alto 자료는 설계안의 충전 주차면을 확인한 것이며 최적 설치 결과나 현재 설치 상태가 아닙니다. 주차장 도면·충전기 주소·해외 충전 수요를 같은 시설의 실측 데이터로 합치지 않습니다.</p><p>환경공단·ACN API는 키가 없어 연결하지 않았습니다. DLP JSON은 다운로드가 403 응답으로 제한되어 원문만 연결합니다. 원문 권리 표시가 있는 도면은 저장소에 재배포하지 않습니다.</p></section><h2>확인한 자료</h2><table><tr><th>자료</th><th>제공자</th><th>확보 상태</th></tr>${catalog.sources.map(s=>`<tr><td><a href="${esc(s.url)}">${esc(s.title)}</a></td><td>${esc(s.provider)}</td><td>${esc(s.access)}</td></tr>`).join('')}</table><p><small>${new Date().toISOString()} 생성 · 데이터 원문 확인 ${catalog.verifiedAt}</small></p></main></html>`);
await writeFile(new URL('summary.json',out),JSON.stringify({generatedAt:new Date().toISOString(),passed,unit:unit.numPassedTests,coverage:coverage.total,e2e:e2e.stats,updateSnapshots:e2e.config.updateSnapshots,sources:catalog.sources.length},null,2));
console.log('HTML report: http://127.0.0.1:5185/reports/mobility/');
