ENGINE_BIND=0.0.0.0:8787
ENGINE_DATA_DIR=/var/lib/leave-engine
ALLOW_REMOTE_DEMO=true
ALLOWED_ORIGINS=https://replace-with-actual-ui.example
# Transparent mock sessions are not authentication. This is a synthetic demo only.
# HTTPS/WSS reverse proxy and an actual persistent volume are required.
