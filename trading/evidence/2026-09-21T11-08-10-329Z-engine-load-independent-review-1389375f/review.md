# 엔진 부하 결과 독립 검토

검토 시각: 2026-09-21 11:05–11:09 UTC. 대상 실행: `2026-09-21T11-01-25-875Z-engine-load-8774ce30`. 소스와 저장된 원시 자료를 읽고 오프라인으로만 검산했다. 테스트·빌드·서비스·새 부하·프로세스 조회/신호를 실행하지 않았으며 메인 엔진/observer, 원본 증거, 소스, 공유 문서는 변경하지 않았다.

**결론: ACK·정산·CPU 정규화·메모리·정리 수치는 원시 자료와 일치한다. `complete:true`는 명령/상태 검증과 정리 완료를 뜻하며 WebSocket 전체 구간 통과를 뜻하지 않는다. 이 실행의 WebSocket 종단 검증은 완료되지 않았다.**

## 실제 입력 식별

- 현재 `scripts/engine-load.mjs` SHA-256: `1DE9249F15993135173B8700703D4A14C839DA777178B7B49CC5C6D30D03C138`.
- 보존된 `engine-load.executed.mjs`는 현재 소스와 바이트 동일하며 SHA-256도 같다.
- 복사된 실제 실행 바이너리 SHA-256: `09bcf75b80a2a85c6a52168d2404f23a230e73b958fc6814daa300650fc5ac56`.
- 모든 검토 입력 파일의 해시, 오프라인 검산 결과와 상세 수치는 같은 폴더의 `review.json`에 기록했다.

## 확인한 수치와 정합성

원시 명령 응답 17,736개를 엔진 순번으로 정렬하여 계정/요청 ID, 유일한 연속 순번·주문 ID, accepted/durable 결과, 주문 1개당 수량 1·가격 1,000을 확인했다. 체결 8,868개도 유일한 연속 ID, maker/taker 연결, 계정 소유, 가격·수량을 대조했다. ACK에 기록된 전체 체결로 계정별 가용/예약 포인트·휴가·누적 주문/체결 수를 재구성했고 warmup 및 각 단계 종료의 네 상태와 일치했다. 각 상태의 실제 보존된 최근 주문·체결 배열도 해당 ACK 이력과 일치했다. 확인한 전체 상태 배열의 범위는 공개 API 제한까지이며 full Core 복구 검증을 새로 수행한 것은 아니다.

HTTP 원시 행은 17,743개이며 별도 WebSocket upgrade 1회를 더한 17,744가 요약과 맞다. warmup 24개와 세 단계 6,000 + 5,952 + 5,760개가 총 17,736개다. 전체 주기 단위로 중단하므로 18,000개를 정확히 채우지 않은 것은 정상이다.

| 동시성 | 명령 / 체결 | 실제 단계 시간 | 엔진 평균 CPU¹ | 엔진 최대 관측 working set | WS 조건 |
|---:|---:|---:|---:|---:|---|
| 6 | 6,000 / 3,000 | 12.700초 | 5.8353% | 18,886,656 bytes | 처음·끝 OPEN, 6,000 프레임 |
| 24 | 5,952 / 2,976 | 12.333초 | 7.3554% | 32,382,976 bytes | OPEN→CLOSED, 5,911 프레임 |
| 96 | 5,760 / 2,880 | 11.291초 | 1.7518% | 34,934,784 bytes | 전체 CLOSED, 0 프레임 |

¹ `Δprocess CPU seconds / Δsampler elapsed seconds / 16 × 100`. 16개 논리 CPU 전체 용량 기준이며, 각각 평균 0.934·1.177·0.280 논리 코어 분량의 CPU 시간에 해당한다. 23·23·21개 유효 구간, 누적 CPU 시간, 가중 평균, 구간 최댓값을 재계산해 요약과 일치함을 확인했다. 해당 구간의 커버리지는 11.865·11.816·10.815초로 단계 전체 시간과 같지 않으며 소스도 이를 별도 필드로 표시한다. 저장된 UTC 취득 시각을 단계 경계와 대조했을 때 범위 밖으로 잘못 배정된 sample은 발견하지 못했다.

73개 전체 resource sample의 엔진 최대 working set/private bytes는 34,934,784 / 32,288,768, 부하 클라이언트는 341,164,032 / 344,788,992로 후속 검산 자료와도 일치했다. 약 500ms 간격 관측 최댓값이며 순간 최고치·메인 서비스/OS 전체 메모리 또는 메모리 누수의 증거로 해석하지 않는다.

## 보고 시 반드시 유지할 경계

1. **[P2 / 해석] 96 단계와 앞 단계의 WS 조건이 다르다.** 24 단계 종료 약 15ms 전에 예상하지 않은 close 1005가 기록됐다. 재연결이 없으므로 96 단계는 HTTP 부하만 측정했다. CPU 감소나 처리량 증가를 동시성 증가만의 효과로 설명하면 안 된다. 24 단계는 WS 일부 구간, 96 단계는 WS 없음으로 표시해야 한다. 현재 단계별 `ready_state`와 frame 수는 이를 정확히 드러낸다.
2. **[P2 / 해석] `sequence_gaps:0`은 받은 앞부분에만 해당한다.** 전체 WS 프레임 11,936개는 순번 0…11,935의 연속 prefix다. 최종 명령 순번은 17,736이므로 이후 5,801개 변경은 이 수신자에게 전달되지 않았다. `unexpected_disconnects:1`과 `caught_up_to_final_event_seq:false`가 올바르게 기록되어 있다. 원인 로그가 없으므로 close 1005만으로 특정 서버 종료 분기를 확정하지 않았다.

`engine-load.mjs:240`의 `complete` 조건은 failure 부재, 상태 검증, 엔진 정상 종료, sampler 종료, 메인 PID/manifest 동일 여부만 사용한다. WS 연결·최종 동기화·오류 수를 조건에 포함하지 않는다. 따라서 표시 문구는 **“bounded 명령/정산 검증 완료; WS 중도 종료”**여야 한다. 현재 summary에는 위 WS 실패 경계와 `all_three_reached_twenty_seconds:false`가 별도로 있으므로 전체 JSON을 읽으면 허위 성공 주장은 아니다. `complete` 단일 값만 녹색 종단 성공 표시로 사용하는 것은 피해야 한다.

## 정리와 검토 범위

저장된 summary의 엔진 종료 코드 0, sampler 종료, 후속 `post-verification.json`의 격리 엔진 22032·부하 클라이언트 10036·sampler 3032 부재를 서로 대조했다. 메인 manifest 해시, 엔진/UI/12봇과 observer 18184의 시작/종료 관측은 동일하다. 이는 저장된 전후 확인 근거이며 이 리뷰에서 현재 프로세스를 재조회하지 않았다.

오프라인 검산 165,812개 조건에서 불일치는 없었다. 새로 확인된 소스/산술 결함은 없다. 위 두 항목은 측정 조건과 성공 범위를 보고할 때의 주의점이며, WS 부하를 모든 동시성에서 성공적으로 처리했다거나 20초씩 측정했다는 결론은 이 증거가 지원하지 않는다.
