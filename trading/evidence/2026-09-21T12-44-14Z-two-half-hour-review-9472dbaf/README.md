# 2.5시간 중간 관찰 독립 검토

2026-09-21 21:45~21:47 KST에 파일만 읽어 검산했다. 기존 엔진·봇·observer를 제어하지 않았고, 빌드·테스트·새 부하·HTTP 요청·live journal 읽기·전체 복구는 수행하지 않았다. 6시간 완료 보고가 아니다.

대상은 `2026-09-21T12-35-11-997Z-observation-analysis-07018e66/analysis.json`이다. 원본 관찰의 19:05:54.979~21:35:08.404 KST 구간과, 별도로 보존한 21:45:43 부근까지의 후속 봇 로그를 구분했다.

## 검산 결과

959개 수치 비교가 일치했다. 1,765개 표본의 단조 시계 간격은 **8,953.4620596초**, 자원 표본은 **295개**, CPU 구간은 **294개**다. 최대 표본 간격은 6,608.4547ms이며 15초 초과 간격은 없다. 표본에서 12봇, 엔진 ready, 15,000,000포인트·15,000시간 합계가 유지됐고, WS 누락·단절은 0이었다. 1,764개 인접 구간 모두 거래량이 증가했다.

CPU 누적 카운터 차이를 실제 구간 시간과 **16개 논리 CPU 전체 용량**으로 나눴다. 엔진·UI·12봇의 합계 CPU 시간은 746.546875초, 가중 평균 사용률은 **0.5211300319%**, 최대 약 30초 구간 평균은 **1.9315020892%**다. 엔진은 407.53125 CPU초, 평균 **0.2844788190%**, 최대 구간 평균 **1.0959342387%**다. 순간 CPU peak나 시스템 전체 CPU 사용률이 아니다.

엔진 표본 최대 working set은 98,332,672바이트, private memory는 97,398,784바이트다. 마지막 표본은 각각 84,381,696 / 96,419,840바이트다. 추적 프로세스 합계 최대는 1,026,215,936 / 1,157,365,760바이트다. Working set 합계는 공유 페이지를 중복 계산할 수 있고, 보존된 거래 이력에 따른 증가만으로 누수를 판정할 수 없다.

## 다섯 번째 스냅샷

현재 엔진 PID 20540 명의 게시 파일이 다섯 개 있으며 약 30분 간격의 자동 저장 설정과 일치한다. 다섯 번째 최종 파일은 `snapshot-00000000000000074813-00000000000001789994123204042200-20540-8.bin`이다.

- 크기: **56,258,024바이트**, payload 56,257,992바이트.
- magic `LVSNAP01`, version 1, flags 0, journal/command/event 순번 **74,813**.
- 헤더 CRC와 전체 payload CRC가 일치하고, 읽기 전후 크기·mtime가 같다.
- SHA-256: `58a7c0295621691e782cf8a6e4dc7974ada8243cf65fa33f38fb5db333c9e30f`.
- 마지막 쓰기 시각: **21:35:23.268 KST**. 최종 파일명으로 존재를 확인한 시각: **21:45:43.998 KST**.

따라서 완전한 frame이 최종 immutable 파일명으로 게시됐음을 확인했다. **mtime는 rename/게시 시각이 아니다.** 현재 엔진 로그에는 성공한 checkpoint의 게시 시각·소요 시간 이벤트가 없어 정확한 게시 시각은 미확정이다. 이 검사는 파일 무결성 검사이며 full Core 불변조건·journal 일치·전체 복구 검증을 대신하지 않는다. 원래 21:35 분석의 마지막 표본은 파일 mtime보다 약 15초 앞서므로, 다섯 번째 파일 확인은 후속 증거다.

## 21:07 이후 지연

새 최대는 **21:08:57.567 KST, bot-04, 782.5103ms, seq 65,570**이었다. 원시 파일 `bot-04-0000.jsonl` 3,609번째 줄, byte offset 2,015,840에 해당한다. 네 번째 스냅샷 mtime보다 **214.687초 뒤**이며 다섯 번째 스냅샷과도 다른 시점이다. 주변 observer는 21:08:49.223에 state 300.3008ms / bot status 288.0572ms를 기록했지만, peak 직전·직후 다른 표본은 수 ms였다. 원인은 특정하지 않았다.

다섯 번째 snapshot 부근에는 **별개의 381.423ms 응답**이 있다: bot-09, seq 74,814, **21:35:23.302 KST**. `bot-09-0000.jsonl` 4,393번째 줄, byte offset 2,969,413이다. 파일 mtime보다 응답 완료가 34ms 뒤라는 시간상 인접만 확인했다. checkpoint 소요 시간 계측 없이 이 지연을 저장 비용으로 단정할 수 없다. ±10초 범위에서 100ms를 넘는 봇 응답은 이 하나다.

원래 분석 cutoff까지 봇 durable 결과는 52,490개, p99 29.1013ms, 최대 1,814.1419ms다. 후속 고정 prefix까지는 56,215개, p99 29.1441ms다. 전체 최대는 이전 21:06의 1.814초가 유지됐고, 21:07 이후 최대가 782.5103ms다. 봇 집계는 19:05:22 시작이라 observer의 명령 증가량 52,295와 시작점이 다르다.

후속 prefix에서 거절 26개는 모두 cancel의 `ORDER_NOT_OPEN`; 이전 21:07 이후 추가분은 4개다. `outcome_unknown`·`reconciled`는 0개다. 21:00~21:15 완성 구간 p99는 162.5608ms이며, 이전에 보고한 미완성 21:00 구간 p99 432.9324ms와 표본 범위가 다르다. 이를 그대로 지연 개선으로 해석하면 안 된다. 21:15~21:30은 p99 25.6998ms, 21:30~21:45는 39.4606ms다.

## 보존 파일

`review.json`에 원본 분석/source SHA, 검산값, 스냅샷 검사, 원본 로그 prefix 길이·해시를 기록했다. `raw-prefixes/`에는 observer·12봇·엔진 로그의 완성된 줄만 고정하여 **14개 파일, 37,462,245바이트**를 보존했다. `cutoff-prefix-manifest.json`은 원래 분석 cutoff의 정확한 prefix offset/hash를 추가한다. 원래 1,765표본 prefix는 1,416,824바이트이며 SHA-256은 `bf9dc01829c3398e630b529a5c5823fb006a68bcac89eb662a4261db11c76b4e`다.

`peak-neighbors.json`, `snapshot-bot-neighbors.json`, `observer-neighbors.json`에 서로 다른 시점의 주변 기록을 분리했다. 원본 전체 저널은 읽지 않았고 복구를 수행하지 않았다. `offline-review.cjs` 실행은 exit 0, 도구 측 경과 1.7502354초였다. 이 폴더 외에는 파일을 쓰지 않았다.
