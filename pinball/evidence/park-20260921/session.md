# DROP LAND 놀이공원 개선 세션

기간: 2026-09-21 한국시간 18:50부터 21:00까지. 사용자가 UI·놀이공원 분위기·지속적인 개선과 검증, main 직접 중간 커밋·푸시를 요청했다. 이후 놀이기구 모양 장애물, 약한 보드 흔들림, 기본 상단 시점 및 다른 시점 선택을 추가로 요청했다. 상단 각도를 유지하며 진행 구간을 따라가는 방식을 사용자가 확인했다.

이 문서는 실제 수행 작업과 관찰 결과의 요약이다. 미실행 항목은 완료로 표시하지 않는다.

## 01 — 19:02, 입장권 UI

- DROP LAND 이름, 크림색 티켓 바탕, 보라색·분홍색·금색 포인트, 행운 패스 그래픽, 탑승권 입력 패널, 네 테마 선택과 결과 카드 구현.
- 네 테마 이름은 별빛 축제·캔디 행성·구름 코스터·원더 가든. 물리 맵 ID 및 충돌 지형은 보존했다.
- 상단 시점을 기본으로 하고 입체·측면 변경을 유지했다.
- 구문 및 정적 빌드 14개 파일 통과. 실제 Chrome 테스트 6/6 통과: 입력 오류·공 수·순위·반복 클릭·잠금·정지·재시작·모바일·전 맵 60개 공·동일 브라우저 재현·반응형·음소거·숨김 탭.
- 60개 공 1배속 측정은 PC 맵별 평균 35.5~54.1 FPS, 모바일 에뮬레이션 48.8 FPS. 독립 전용 장비가 아니며 이후 놀이기구 그래픽의 성능은 별도로 검증한다.
- `01-ui-browser-results.json`, `01-ui-browser.log`, PC·모바일 캡처를 보존했다.

다음 묶음: 실제 3D 놀이기구 모델, 약한 실제 물리 흔들림, 조작 가독성·성능 개선. 아직 이 단계에서 완료했다고 간주하지 않는다.

## 02 — 19:09, 3D 놀이기구

- 원형 범퍼를 작은 회전목마, 회전 막대를 찻잔 놀이기구, 이동 막대를 3칸 열차로 구성했다. 사탕 핀·성문·가랜드·풍선·관람차·구름·전구 레일을 추가했다. 코스 밖 장식에 숨은 충돌은 없다.
- 반복 핀·나무·전구는 인스턴스 렌더링으로 묶고 맵 교체 때 해당 GPU 자원을 해제했다.
- 첫 카메라 검증은 일시정지 레이어가 시점 버튼을 가려 실패. 덮개 위에 시점 버튼을 배치해 실제 클릭으로 재검증 통과. 실패 로그도 별도 보존했다.
- 상단을 기본으로 PC·모바일 60개 공 3배속, 정지 중 3가지 시점 변경, 실제 도착·순위와 같은 Chrome 재현 모두 통과. PC 48.6 FPS, 모바일 에뮬레이션 52.9 FPS. 시간대·부하가 다른 측정이므로 이전 수치와 엄밀한 성능 우열을 주장하지 않는다.
- 4개 맵 각각 실제 경기·같은 브라우저 재현 통과. 40회 맵 변경 후 같은 맵의 geometry 158, texture 9, shader program 16으로 시작/종료 동일. 일반적인 장기 메모리 누수 부재를 보장하는 시험은 아니다.
- UI 커밋 a9b7a11의 첫 push는 원격 신규 변경 때문에 non-fast-forward로 실패했다. pinball 외의 두 신규 커밋을 fetch/merge하고 a1ad801에서 원격 main 해시 일치 확인. 기존 원격 파일을 덮어쓰거나 강제 푸시하지 않았다.

## 03 — 19:20, 상단 액션과 살랑 보드

- 회전 막대마다 서로 다른 절대 속도와 양방향 회전, 보드의 약한 실제 가속도(7초부터 9초 간격, 1.8초) 적용. 기본 켜짐이며 시작 전 변경 가능. 참가자·순위·당첨자를 참조하지 않는다.
- 실제 접촉의 파동, 공의 짧은 잔상, 회전목마의 충돌 반응 추가. 연출 줄이기는 시각 효과만 끄며 물리는 유지한다.
- 원형 맵 1개 공/시드 5에서 넓게 진동하며 끝나지 않는 실제 실패 발견. 위치 정체뿐 아니라 8.5초간 아래로 진행하지 못한 경우도 탈출 충격 적용. 회귀 사례 및 일반/흔들림 각각 120회 총 240경기, 단위 테스트 17/17 통과. 순간이동이나 가짜 완주 판정 없음.
- PC·모바일 60개 공/3배속, 실제 흔들림, 설정 잠금, 정지·연출 변경·재개, 순위·승자·같은 Chrome 재현, 흔들림 끄기 통과. 테스트 초안은 snapshot의 time을 raceTime으로 잘못 참조해 시간 초과. 테스트 오류를 고친 후 통과했으며 실패 JSON 보존.
- 효과를 켠 최대 수량 표본은 PC 29.0 FPS/JS 평균 10.12ms, 모바일 에뮬레이션 28.4 FPS/11.57ms. 추가 렌더링 최적화가 필요하며 이를 다음 단계로 진행한다.
- 이전 놀이기구 커밋 5d61f72는 Fork push 후 원격 main 해시 일치 확인.

## 04 — 19:28, 상단 확대와 렌더링 최적화

- 고정 상단 카메라의 코스 배율 확대. 이름표 실제 크기를 기준으로 겹침을 피하는 간결 모드, 이름 모두 보기 전환 추가. 정지 중 전환해도 물리 스냅샷 불변 및 PC·모바일 이름표 비겹침 확인.
- 정적인 장식 메시를 재질별로 묶고, 동적인 회전·이동 놀이기구는 별도 유지. 회전목마 충돌 반응은 지붕에 적용한다.
- 실제 Chrome 60개 공/3배속/연출 켜짐: PC·모바일 에뮬레이션 각각 60.0 FPS 표본. JS 평균 4.94ms/4.73ms. 흔들림·정지·재개·연출 변경, 실제 도착과 같은 Chrome 재실행 일치 통과. 결과는 해당 환경/시드의 표본이며 모든 기기의 보장이 아니다.
- 03 커밋 97c2444 push는 원격에 다른 프로젝트의 5개 커밋이 추가돼 첫 시도 거절. pinball 변경이 없는 것을 확인 후 정상 병합. 680a1ef에서 로컬·원격 main 동일 확인. 강제 push 없음.
- 4개 맵 각 12개 공 실제 완주·재현 통과. 40회 맵 교체 후 동일 맵 geometry 63, texture 9, program 17, draw calls 41로 시작/종료 동일.

## 05 — 19:43, 네온 놀이기구와 큰 당첨자 안내

- 회전목마·우주선·범퍼카·찻잔·네 날개 풍차·바이킹·열차·범퍼 셔틀, 세 구간 네온 레일/별/바닥 광륜 추가. 풍차는 교차하는 실제 충돌 막대 2개, 바이킹은 왕복 각도와 순간 각속도를 충돌 계산에 사용한다.
- 당첨 시 중앙의 큰 이름 카드, 잠정/확정 표시, 재경기/전체 순위 버튼 추가. PC 66px/모바일 39px 실제 글자 크기 확인.
- 개발 파일을 현재 미리보기에서 차례로 수정하던 중 새 풍차 물리와 이전 렌더러가 함께 로드돼 19:31에 빈 보드 오류 발생. 원인은 이전 렌더러가 물리 막대 수와 메시 수를 일대일로 가정한 점. 모델별 자세 함수로 수정하고 현재 미리보기 복구를 직접 확인했다. 이후 후보 개발을 4189 서버로 분리했으며 검증한 버전별 자원을 고정하여 4188 미리보기에 제공한다.
- 렌더링 예외도 경기를 자동 정지하고 오류 안내/화면 복구를 제공. Chrome에서 실제 drawElements 예외를 주입한 후 위치·순위 불변, 화면 복구, 재개, 같은 Chrome 완주 재현까지 PC·모바일 모두 통과. 오류를 숨기거나 임의로 완주시키지 않는다.
- 변경한 지형의 물리/규칙 18/18, 흔들림 유무 각각 120회 총 240회 완주·경계·중복 집계 검증 통과. 기존 진동 정체 사례는 이전 지형을 복원해 회귀 검증했다.
- 60개 공/3배속/연출 켜짐 표본: PC 59.7 FPS, JS 평균 4.94ms; 모바일 Chrome 에뮬레이션 60.0 FPS, 4.74ms. 실제 기기 보장은 아니다. 네온/충돌/흔들림/정지/이름표/같은 Chrome 재현 모두 통과.
- 04 커밋 fae9aad는 원격 main 동일 해시 확인.

## 06 — 19:49, 3~5초 사이 무작위 좌우 흔들림

- 사용자 요청을 5초 고정에서 3~5초 무작위 간격으로 변경. 첫 흔들림도 경기 시작 후 3~5초이며, 1.8초의 부드러운 좌우 힘은 기존 세기를 유지한다. 독립된 시드 기반 일정으로 참가자·순위와 경기 탈출 RNG를 참조하지 않는다.
- 단위 검증 18/18 통과. 간격 범위·같은 시드 일정 재현·서로 다른 시드 일정 차이·부드러운 시작/끝·모든 공 동일한 힘·정지·240경기 완주와 고유 도착 순위 포함.
- 05 기능 커밋 24e7256은 원격 main 동일 해시 확인.
- PC·모바일 60개 공/3배속 표본 각각 약 60.0 FPS, JS 평균 5.06ms/4.49ms. 정지·재개·연출 전환·흔들림 끄기·실제 도착 순서와 같은 Chrome 재현 통과.

## 07 — 20:00, 즉시 종료 규칙과 크게 보기

- 후속 사용자 요청에 따라 첫 번째/N번째는 해당 통과 순간 종료, 마지막은 한 공이 남는 순간 종료로 변경했다. 마지막 공은 미도착 상태이며 도착 목록에 추가하지 않는다. 큰 결과 카드에서 마지막 남은 공임을 명시한다.
- 실제 세부 통과 시각에서 남은 공 위치와 시뮬레이션 시간을 보간해 멈춘다. 같은 스텝의 후속 도착은 집계하지 않는다. 한 공/마지막 규칙은 게이트가 열릴 때 확정한다.
- 4개 맵·1/7/20개·3가지 규칙, 동일 물리 전체 코스의 도착 접두 기록 일치, 동시 도착 우선값, 1/3배속의 종료 위치·시간·승자 동일: 새 단위 테스트 4/4 통과.
- PC·모바일 실제 첫 번째/N번째/마지막 총 6경기, 즉시 종료·정지 상태 유지·큰 이름·실제 도착 목록·동일 Chrome 스냅샷 재현 통과. 한 공/40자 이름도 두 환경에서 확인했다.
- 크게 보기 모드: 경기장과 주요 버튼을 뷰포트에 맞추고 원래 화면/Escape로 복귀. PC·모바일에서 정지 중 전환해도 상태 불변, 결과 목록 복귀, 잘못된 입력 때 설정으로 복귀 확인. 이 기능 검증 뒤 즉시 종료 규칙을 더했으므로 최종 종합 검증을 이어간다.
- 06 커밋 1e3ac2c 원격 main 해시 일치 확인.

## 08 — 20:06, 변경 전 장기 안정성 기준

- 06 버전 물리(자석·대포와 새 코스 변경 전)로 4개 맵 × 6개 공 수 × 흔들림 켜기/끄기 × 시드 50개, 총 2,400경기 검증 통과. 1,000.5초 실행. 이 결과를 이후 새 장치 버전의 장기 검증으로 간주하지 않는다.
- 07 커밋 d04ed02는 Fork 푸시 성공 뒤 원격 main 해시 일치를 확인했다.

## 09 — 20:17, 자석·대포와 맵별 결승부

- 기본 속도 1.5배, 선택 1·1.5·2·3배. 자석은 접촉 위치에서 공을 0.5~1초 잡고 방출, 대포는 실제 V자 깔때기 진입 공을 잡고 무작위로 회전한 뒤 가속 발사한다. 모든 시간은 물리 시간이며 배속 영향을 받는다. 장치당 동시 공 하나, 방출 후 2초 재포획 방지. 이름·순위를 참조하지 않고 순간 이동하지 않는다.
- 구름 코스터 경사로 7→5개, 빈 구간에 원형 범퍼와 네 날개 풍차 추가. 캔디 행성 마지막 양쪽에 범퍼와 핀 추가. 원더 가든 네 출구에 실제로 통로를 열고 막는 꽃잎 회전문 추가. 별빛 축제 마지막 사선 사이 회전판 및 중앙 단일 구멍으로 변경.
- 첫 물리 검사는 구름 코스터의 기존 좁은 좌측 구간에 대포를 배치해 90초 제한에 걸리는 실패 4개. 실패 로그 `09-physics.log` 보존. 대포를 넓어진 중앙 구간으로 옮긴 후 26/26 통과(`09b-physics.log`). 이후 별빛 단일 골인까지 반영한 29/29 통과(`09c-physics.log`). 총 240경기 일반/흔들림 코스 검증과 40경기 장치 자연 진입, 규칙 종료/재현, 포획 시간/정지/고속 충돌 검증 포함.
- PC·모바일 실제 60개 공 시연에서 자석 포획·대포 회전 중 정지·배속 변경·재개·큰 결과·재시작 통과. 같은 시드에서 실제 포획 30회, 대포 발사 24회, 자석 방출 6회. 마지막 규칙은 59개만 실제 도착 기록, 남은 공 당첨. 전체 최종 스냅샷이 같은 Chrome 물리 재실행과 정확히 일치(`09-devices-browser-results.json`).
- 최신 네 맵 각각 실제 경기와 Chrome 재현, 맵 변경 40회 후 GPU 자원 수 동일 통과(`09c-rides-results.json`). 정적 빌드 17파일 및 모바일 WebGL/Canvas 호환 화면 실제 경기 통과. 전체 사용 흐름·추가 성능 검사는 계속 진행 중이며 여기서는 아직 완료로 기록하지 않는다.

## 10 — 20:21, 기본 속도와 정지 가독성

- 09 커밋 97622c1 main 푸시 후 원격 동일 해시 확인. 전체 PC/모바일 사용 흐름 6/6 및 4개 맵 60개 공 검증 통과(`09-full-browser-results.json`). 기본1.5배속·연출 줄임 표본은 PC 4맵/모바일 약60FPS.
- 정지 시 전체 화면을 흐리던 덮개를 작은 안내로 변경. 320·390·1440 너비에서 주요 버튼이 화면 안에 있고, 정지 안내 면적이 보드의 3.5~9.3%임을 확인. Space 정지/재개, 입력창의 Space 유지, 정지 중 시점 변경 상태 불변 통과(`10-pause-results.json`).
- 09 물리/장치 버전 60개 공·3배속·효과 켜짐은 PC 59.6FPS/JS6.07ms, 모바일 에뮬레이션54.7FPS/6.42ms 표본. 이후 다중 장치 버전 성능을 이 수치로 대신하지 않는다.

## 11–12 — 20:38, 골인/리턴과 다중 포획

- 사용자 변경에 따라 최종 구성은 별빛 축제·구름 코스터 골인1개, 캔디 행성·원더 가든 골인1개+리턴1개. 중간 구멍을 추가 골인으로 처리하던 후보안은 공개하지 않고 폐기했다. 리턴은 도착 순위에 포함하지 않고 공을 중간 지점 위에서 다시 내려보낸다. 정지 중 이동/낙하 연출도 정지한다.
- 맵마다 대포2개·자석2개. 캔디 대포를 좁은 하단 좌측에서 중앙680/1730 지점으로 이동. 장치가 여러 공을 저장하며 공마다 독립적인0.5~1초 대기 후 방출한다. 자석은 공을 들어 올리고 대포는 내부 저장으로 입구를 비운다. UI에 현재 수량 표시. 대포는 방출 순간의 실제 포신 방향으로 발사한다.
- 최초 리턴 시험에서 동일 공6회 리턴으로150초 제한 초과, 캔디 재진입 위치가 범퍼와 겹치는 실패를 발견(`11-physics.log`). 재진입 위치를 넓은310/1200으로 옮기고60초 피날레 유도 레일을 추가했다. 첫 덮개안은 이미 통로 안에 있던 공을 가둬 실패(`12-physics.log`). 덮개를 제거하고 상단 진입로만 막아 기존 공은 리턴 구멍으로 빠져나오게 수정했다. 골인 결과를 임의로 만들지 않는다.
- 최종 물리·규칙34/34 통과(`12b-physics.log`,251.9초). 240경기 일반/흔들림 및40경기 장치 자연 진입, 첫/N/마지막 종료 접두 기록,5개 동시 포획 독립 타이머, 포획/리턴 정지, 빠른 충돌, 리턴 반복 후 단1회 골인 집계,60초 전환 포함.
- PC·모바일 실제60개 공에서 자석3개/대포4개 동시 포획 관찰. 포획 중 정지·재개 및 전체 결과 스냅샷이 같은 Chrome 재실행과 일치(`12b-multi-device-browser-results.json`). 캔디/원더×PC/모바일4경기는 실제 리턴16회/23회, 순위20개가 모두 GOAL 통과이고 재현 일치(`12b-return-browser-results.json`).
- 빌드18파일과 모바일 WebGL/Canvas 실제 경기 통과. 네 맵 실제 경기 및40회 전환 후 그래픽 자원 수 동일 통과(12-rides는 피날레 덮개를 제거하기 전 시드별 검사이며 최신 물리 장기 안정성 증거로 사용하지 않는다).
- 최신 소스 해시를 기록한 추가480경기(시드11~20,4맵,1/2/7/20/40/60개,흔들림 켜기/끄기) 검사는 진행 중이다. 완료 전에는 통과로 기록하지 않는다.
