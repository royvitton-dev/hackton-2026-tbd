import fs from 'node:fs/promises';
import path from 'node:path';
const read=async file=>{try{return JSON.parse(await fs.readFile(file,'utf8'));}catch{return null;}};
const unit=await read('reports/unit/results.json'),coverage=await read('reports/coverage/coverage-summary.json'),browser=await read('reports/e2e/results.json'),checks=await read('reports/checks.json');
const comparison=await read('reports/golden/comparison/results.json');
const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const tests=[];
function collect(suite){for(const spec of suite.specs||[])for(const test of spec.tests||[])tests.push({title:spec.title,project:test.projectName,status:test.status,duration:test.results?.at(-1)?.duration||0});for(const child of suite.suites||[])collect(child);}
// Reuse passing final-build app scenarios; later runs replace matching cases.
const finalAppRun=await read('reports/sponsor-debug/audio-iteration-7/results.json');
for(const suite of finalAppRun?.suites||[])collect(suite);
for(const suite of browser?.suites||[])collect(suite);
const latest=new Map(tests.map(test=>[`${test.project}:${test.title}`,test]));tests.splice(0,tests.length,...latest.values());
if(comparison){for(let i=tests.length-1;i>=0;i--)if(tests[i].project==='golden')tests.splice(i,1);for(const suite of comparison.suites||[])collect(suite);}
const style=`*{box-sizing:border-box}body{margin:0;background:#f6f7f1;color:#35432f;font:14px/1.7 system-ui,sans-serif}main{max-width:1080px;margin:45px auto;padding:0 26px}a{color:#ed653f;text-decoration:none}header{border-bottom:1px solid #dfe6d5;padding-bottom:25px}small{letter-spacing:2px;color:#92a17f}h1{font-size:37px;letter-spacing:-1.5px;margin:12px 0 5px}h2{font-size:19px;margin:30px 0 15px}p{color:#859375}nav{display:flex;gap:18px;margin:23px 0;flex-wrap:wrap}.cards{display:grid;grid-template-columns:repeat(4,1fr);gap:14px}.card{border:1px solid #dce4d1;padding:21px;border-radius:11px;background:#fffef8}.card>b{font-size:32px;display:block;color:#ed653f}.card>span{font-size:11px;color:#8d9b7d}table{border-collapse:collapse;width:100%;background:#fffef8;font-size:12px}td,th{text-align:left;border-bottom:1px solid #e7eddd;padding:12px;word-break:break-word}th{background:#edf2e5;color:#849371}pre{background:#eaf0e0;border-radius:7px;overflow:auto;padding:18px;font-size:11px}.ok{color:#548140}.fail{color:#c7583d}.note{background:#edf2e4;border:1px solid #dde7cf;border-radius:10px;padding:18px 22px;margin:20px 0;font-size:12px}.gallery{display:grid;grid-template-columns:1fr 1fr;gap:20px}.gallery figure{margin:0;background:white;border:1px solid #e0e7d5;border-radius:9px;overflow:hidden}.gallery img{width:100%;display:block}.gallery figcaption{padding:13px;font-size:12px}footer{border-top:1px solid #dde5d2;margin-top:35px;padding:20px 0;color:#8b987a;font-size:11px}@media(max-width:700px){.cards,.gallery{grid-template-columns:1fr 1fr}main{margin-top:25px;padding:0 16px}h1{font-size:28px}td,th{padding:9px;font-size:10px}nav{gap:12px;font-size:11px}}`;
const page=(title,content,prefix='../')=>`<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} · BREW RACERS</title><style>${style}</style></head><body><main><header><small>BREW RACERS / QUALITY REPORT</small><h1>${esc(title)}</h1><p>실행한 검사 결과 · ${esc(new Date().toISOString())}</p></header><nav><a href="${prefix}index.html">전체 결과</a><a href="${prefix}unit/index.html">단위 테스트</a><a href="${prefix}coverage/index.html">커버리지</a><a href="${prefix}e2e/index.html">브라우저 테스트</a><a href="${prefix}golden/index.html">골든 테스트</a><a href="${prefix}audio/index.html">음향 샘플</a><a href="${prefix}characters/index.html">캐릭터</a><a href="${prefix}podium/index.html">우승 시상대</a><a href="${prefix}coffee/index.html">커피차</a><a href="${prefix}sponsors/index.html">광고·스폰서</a></nav>${content}<footer>React · TypeScript · Three.js WebGL · Vitest · Playwright / ${esc(process.version)}</footer></main></body></html>`;
const write=async(file,content)=>{await fs.mkdir(path.dirname(file),{recursive:true});await fs.writeFile(file,content);};
const unitCases=(unit?.testResults||[]).flatMap(file=>(file.assertionResults||[]).map(t=>({...t,file:path.basename(file.name)})));
const rows=unitCases.map(t=>`<tr><td>${esc(t.file)}</td><td>${esc(t.fullName||t.title)}</td><td class="${t.status==='passed'?'ok':'fail'}">${esc(t.status)}</td><td>${(t.duration||0).toFixed(1)} ms</td></tr>`).join('');
await write('reports/unit/index.html',page('단위 테스트',`<div class="note">${unit?.numPassedTests??0} / ${unit?.numTotalTests??0}개 통과. 6개 트랙의 8명 완주, 아이템과 방어, 발사 지연·충돌 시점 방어·연출 재현성, 시드 재현, 꼴찌 선정, 하이라이트, 로그 검증, 음성 특징, IndexedDB 저장, 음악 구성·음향 상태 제어, Nintendo 두상 8종, 커피차의 바리스타·손님 배정과 잔 전달 시간축, 6개 맵 광고 건물의 도로 이격·서로 간격·방향을 검사합니다.</div><p><a href="coverage.log">최종 단위·커버리지 출력</a> · <a href="sponsor-iteration-1.log">광고 건물 간격 최초 실패 및 수정 근거</a></p><table><tr><th>파일</th><th>검사</th><th>결과</th><th>시간</th></tr>${rows}</table>`));
const goldens=tests.filter(t=>t.project==='golden'),e2e=tests.filter(t=>t.project==='e2e');
let baselineFiles=[];const baselineRoot='tests/e2e/golden.spec.ts-snapshots';try{baselineFiles=await fs.readdir(baselineRoot);}catch{}
for(const name of baselineFiles)if(name.endsWith('.png')){await fs.mkdir('reports/golden/baselines',{recursive:true});await fs.copyFile(path.join(baselineRoot,name),path.join('reports/golden/baselines',name));}
await write('reports/golden/index.html',page('화면 골든 테스트',`<div class="note">${checks?.final?.status==='stopped-incomplete'?'최종 비교 미완료. 아래 기준에는 최신 렌더링 수정 이전 화면이 포함되어 있어 통과 증거로 사용할 수 없습니다.':'직접 검토한 초기 화면을 기준으로 저장하고, 별도 실행에서 픽셀 차이를 비교합니다.'} 외부 디자인 시안과의 일치도를 측정하지 않습니다. Chrome + SwiftShader, 고정 뷰포트, 모션 감소. 최종 독립 비교는 빌드 preview 서버에서 수행합니다. 허용 픽셀 차이 비율: 0.6%.</div><table><tr><th>검사</th><th>결과</th><th>시간</th></tr>${goldens.map(t=>`<tr><td>${esc(t.title)}</td><td class="${t.status==='expected'?'ok':'fail'}">${esc(t.status)}</td><td>${(t.duration/1000).toFixed(2)} s</td></tr>`).join('')||'<tr><td>비교 미실행</td></tr>'}</table><p><a href="comparison/index.html">독립 비교 Playwright 보고서</a> · <a href="comparison/results.json">독립 비교 원본 JSON</a></p><h2>검토한 기준 이미지</h2><div class="gallery">${baselineFiles.filter(f=>f.endsWith('.png')).map(f=>`<figure><a href="baselines/${esc(f)}"><img src="baselines/${esc(f)}" alt="${esc(f)}"></a><figcaption>${esc(f)}</figcaption></figure>`).join('')}</div>`));
await write('reports/sponsor-debug/index.html',page('골든 기준 생성 중 개발 서버 연결 단절',`<div class="note">브라우저 기능 14개 통과 후 커피차 골든을 준비하던 중 개발 서버 HMR 연결이 끊기고 자동 새로고침이 발생했습니다. Trace에서 연결 단절 뒤 페이지 실행 문맥이 사라진 것을 확인했습니다. 연결이 끊긴 원인 자체는 미확정입니다.</div><p>해당 환경 오류는 소스나 비교 허용 오차를 바꾸지 않고 HMR이 없는 빌드 preview 서버에서 남은 기준을 생성하여 회복했습니다. 이후 추가 요청에 따라 회사별 사업 그래픽을 적용하고 최종 회귀를 다시 실행했습니다. 독립 비교 최종 결과는 <a href="../golden/comparison/index.html">골든 비교 보고서</a>에서 확인할 수 있습니다. 앞서 통과한 기능 14개 결과는 그대로 보존했습니다.</p><p><a href="iteration-3-results.json">실패를 포함한 최초 실행 JSON</a> · <a href="error-context.md">오류 문맥</a> · <a href="trace.zip" download>Trace</a></p><img style="width:100%" src="test-failed-1.png" alt="개발 서버 연결이 끊긴 시점의 화면">`));
await write('reports/sponsor-debug/audio-iteration-7/index.html',page('음소거 출력 측정 재검증',`<div class="note">최종 빌드의 app 시나리오 7개가 통과한 뒤 음소거 검사에서 0.010761458426713943이라는 동일한 측정값이 반복됐습니다. 당시 AudioContext 상태와 시계 진행은 기록하지 않아 정확한 원인을 확정하지 않았습니다. 앱은 음소거 상태였고 모든 소스를 끊는 코드를 확인했습니다.</div><p>측정 프로브에 0 값의 ConstantSource를 더해 게임 소스가 끊겨도 측정 그래프를 유지하고, 오디오 문맥이 실행 중인지 확인하도록 보완했습니다. 0 입력은 잔여음을 지우지 않으며 기존 음량 기준 0.0001을 그대로 사용합니다. <a href="https://www.w3.org/TR/webaudio-1.0/">Web Audio 명세</a>는 정지된 문맥에서 AnalyserNode가 같은 데이터를 반복 반환함을 명시합니다. 이것만으로 당시 문맥이 정지됐다고 단정하지 않습니다.</p><p><a href="results.json">원본 JSON (통과한 app 7개 포함)</a> · <a href="trace.zip">실패 Trace</a> · <a href="error-context.md">오류 문맥</a> · <a href="../../e2e/index.html">후속 검증</a></p><img style="width:100%" src="test-failed-1.png" alt="음소거 검사 실패 당시 화면">`,'../../'));
const knownIssues=checks?.knownIssues||[];
const knownNote=knownIssues.length?`<div class="note fail"><strong>기존 미해결 검사 ${knownIssues.length}개 — 전체 프로젝트 완료 판정은 보류합니다.</strong>${knownIssues.map(issue=>`<p>${esc(issue.detail)}</p><p>필요한 사용자 입력: ${esc(issue.requiredUserInput)}</p><a href="${esc(issue.evidence)}">보존한 실패 증거</a>`).join('')}</div>`:'';
const stopNote=checks?.final?.status==='stopped-incomplete'?`<div class="note fail"><strong>사용자 지정 중단 조건에 도달하여 미완료 상태입니다.</strong><p>${esc(checks.final.blocker)}</p><p>필요한 사용자 입력: ${esc(checks.final.requiredUserInput)}</p><a href="replay-debug/index.html">최신 실패 증거 보기</a></div>`:'';
const cov=coverage?.total,passed=tests.filter(t=>t.status==='expected').length,scopePassed=unit?.success===true&&e2e.length===(checks?.final?.e2eExpected??14)&&goldens.length===(checks?.final?.goldenExpected??20)&&passed===tests.length&&checks?.final?.baselineCompared===true&&Boolean(comparison)&&checks?.build?.exitCode===0,allPassed=scopePassed&&knownIssues.length===0;
await write('reports/index.html',page('작은 레이스, 꼼꼼한 검증.',`${stopNote}${knownNote}<div class="note ${scopePassed?'ok':'fail'}"><strong>${scopePassed?'이번 추가 기능 및 실행한 회귀 검사 통과':'검사 미실행 또는 실패 항목이 있습니다.'}</strong><br>단위 ${unit?.numPassedTests??0}/${unit?.numTotalTests??0} · 브라우저 ${e2e.filter(t=>t.status==='expected').length}/${e2e.length} · 화면 골든 ${goldens.filter(t=>t.status==='expected').length}/${goldens.length} · 빌드 ${checks?.build?.exitCode===0?'통과':'미확인'}</div><div class="cards"><a class="card" href="unit/index.html"><b>${unit?.numPassedTests??'—'}</b><span>단위 테스트 통과</span></a><a class="card" href="coverage/index.html"><b>${cov?.statements?.pct??'—'}%</b><span>측정한 핵심 모듈의 문장 커버리지</span></a><a class="card" href="e2e/index.html"><b>${e2e.filter(t=>t.status==='expected').length}/${e2e.length}</b><span>브라우저 시나리오</span></a><a class="card" href="golden/index.html"><b>${goldens.filter(t=>t.status==='expected').length}/${goldens.length}</b><span>화면 골든 비교</span></a></div><h2>범위와 한계</h2><table><tr><th>항목</th><th>측정 결과</th></tr><tr><td>커버리지</td><td>core 모듈(레이스·연출·음성·저장·캐릭터 목록), audio 모듈(음악·합성·효과음·제어) 단위 검사. UI·WebGL·브라우저 녹음 API는 이 비율에 포함하지 않습니다. 문장 ${cov?.statements?.pct??'—'}%, 분기 ${cov?.branches?.pct??'—'}%, 함수 ${cov?.functions?.pct??'—'}%, 라인 ${cov?.lines?.pct??'—'}%.</td></tr><tr><td>음성</td><td>합성 오디오와 모의 받아쓰기로 실제 Web Audio·MediaRecorder·저장·화자 주문 흐름을 검증. 실제 사람의 인식 정확도·물리 마이크·제공업체 STT 연결은 자동 검사하지 않았습니다.</td></tr><tr><td>음향</td><td>실제 AudioContext 출력으로 재생·정지·음소거를 확인. OfflineAudioContext에서 3곡·7종 효과음을 렌더해 무음·클리핑·동일 PCM 여부 검사. 합성 WAV는 음향 보고서에서 재생할 수 있습니다.</td></tr><tr><td>시상대</td><td>실제 1위가 트로피를 받으며, 2명 참여 시 3위를 만들지 않습니다. 트로피 상승·일시정지·9초 후 커피차 전환·다시 보기·모바일 배치 검사. 커피차의 대사·따르기·손님 전원 배정·잔 전달·하이라이트 전환도 확인합니다.</td></tr><tr><td>브라우저</td><td>Google Chrome headless + SwiftShader. 다른 브라우저와 실제 모바일 GPU 성능은 별도 검증 대상.</td></tr><tr><td>포트 충돌</td><td>${esc(checks?.port?.detail||'미확인')}</td></tr><tr><td>의존성</td><td>${esc(checks?.audit?.detail||'미확인')}</td></tr><tr><td>빌드</td><td>${esc(checks?.build?.detail||'미확인')}</td></tr></table><p>앞선 기준 생성 실행에서 개발 서버 연결 단절 1건이 발생했습니다. 당시 원본과 Trace를 별도 보존했습니다. 최종 기능 14개는 같은 빌드에서 통과한 app 시나리오 7개와 음향 측정 프로브를 보완한 후 나머지 기능 7개 결과를 합친 것입니다. 골든은 빌드 서버에서 별도 비교합니다. <a href="sponsor-debug/index.html">환경 오류와 회복 근거</a>.</p><h2>수정과 검증 기록</h2><table><tr><th>회차</th><th>원인 및 조치</th><th>결과</th></tr>${(checks?.iterations||[]).map(i=>`<tr><td>${i.number}</td><td>${esc(i.detail)}</td><td>${esc(i.result)}</td></tr>`).join('')}</table><h2>검증 과정의 브라우저 캡처</h2><p>시상대·커피차·결과 화면은 이번 검증에서 촬영했습니다. 각 화면의 기준 이미지와 비교 결과는 골든 보고서에 있습니다. 기존 실패는 별도 보존했습니다.</p><div class="gallery">${['podium','coffee-truck','coffee-truck-eight-mobile','results'].map(name=>`<figure><a href="screenshots/${name}.png"><img src="screenshots/${name}.png" alt="${name}" loading="lazy"></a><figcaption>${name}</figcaption></figure>`).join('')}</div><h2>다시 실행하기</h2><pre>npm run dev\nnpm run build\nnpm run test:coverage\nnpm run test:e2e -- --grep-invert 'deterministic pause'\nnpm run preview -- --port 5175  # 별도 터미널에서 유지\nnpm run test:golden\nnpm run reports</pre><p>위 명령은 이전에 중단한 엄격한 리플레이 픽셀 검사 1개를 제외합니다. 전체 명령은 <code>npm run test:e2e</code>이며 기존 미해결 검사를 포함합니다.</p><p><a href="replay-example.json" download>검증 레이스 JSON 로그</a> · <a href="build/index.html">빌드·환경 검사 원본</a></p>`,''));
await write('reports/build/index.html',page('빌드 및 실행 환경',`<pre>${esc(JSON.stringify(checks,null,2))}</pre><p><a href="build.log">빌드 출력</a> · <a href="audit.json">의존성 검사 원본</a></p>`));
if(checks?.final?.status==='stopped-incomplete')await write('reports/replay-debug/index.html',page('리플레이 화면 재현성 — 검증 중단',`<div class="note fail">${esc(checks.final.blocker)}</div><p>원인 미확정: PNG를 디코딩한 픽셀 해시도 달라지므로 압축 파일 차이만으로 설명할 수 없습니다. 단위 검사에서 시간축 재현성은 통과했으며 화면 합성·렌더링 상태의 추가 조사가 필요합니다.</p><p>${esc(checks.final.requiredUserInput)}</p><p><a href="error-context.md">실패 오류 문맥</a> · <a href="trace.zip" download>Playwright trace 내려받기</a></p><img style="width:100%" src="test-failed-1.png" alt="실패 직후 아이스 피격 화면"><pre>npx playwright show-trace reports/replay-debug/trace.zip</pre>`));
const levels=await read('reports/audio/levels.json')||[];
const audioNames={lobby:'로비 · 준비하는 시간',race:'레이스 · 두 바퀴 질주',results:'결과 · 우승 축하','launch-bean':'원두 미사일 발사','hit-bean':'원두 폭발','hit-ice':'얼음 피격','hit-storm':'번개 피격','blocked-bean':'공격 방어','item-boost':'부스트 사용','item-shield':'실드 사용'};
await write('reports/audio/index.html',page('배경음과 효과음',`<div class="note">외부 음원 파일 없이 Web Audio로 연주하는 자체 제작 음악입니다. 실제 Chrome의 OfflineAudioContext로 렌더링한 샘플 10개입니다. 모든 샘플의 RMS &gt; 0.002, 최대 진폭 &lt; 0.98, PCM 해시가 서로 다름을 검사했습니다. Nintendo 게임 원곡을 사용하지 않습니다.</div><p>첫 클릭 또는 키 입력 후 배경음이 시작됩니다. 로비·레이스·시상식 음악이 전환되고 마지막 바퀴는 빨라집니다. 사운드 설정은 기억하며 일시정지·마이크 입력 중 음악과 효과음이 멈춥니다.</p><table><tr><th>음향</th><th>듣기</th><th>RMS</th><th>최대 진폭</th></tr>${levels.map(s=>`<tr><td>${esc(audioNames[s.name]||s.name)}</td><td><audio controls preload="none" style="width:min(100%,310px)" src="samples/${esc(s.name)}.wav"></audio></td><td>${s.rms.toFixed(4)}</td><td>${s.peak.toFixed(4)}</td></tr>`).join('')}</table><p><a href="levels.json">측정값 원본</a></p>`));
const featureGallery=filter=>`<div class="gallery">${baselineFiles.filter(f=>f.endsWith('.png')&&f.includes(filter)).map(f=>`<figure><a href="../golden/baselines/${esc(f)}"><img src="../golden/baselines/${esc(f)}" alt="${esc(f)}"></a><figcaption>${esc(f)}</figcaption></figure>`).join('')}</div>`;
await write('reports/characters/index.html',page('Nintendo 캐릭터 8종',`<div class="note">마리오·루이지·피치·요시·쿠파·키노피오·동키콩·로젤리나를 직접 만든 3D 두상으로 구현했습니다. 같은 모델을 프로필·카트·시상대에 사용합니다. 프로필에서 캐릭터를 선택하고 새로고침 후 보존되는지 브라우저에서 확인했습니다.</div>${featureGallery('nintendo-picker')}`));
await write('reports/podium/index.html',page('우승 트로피 시상대',`<div class="note">최종 순위의 상위 3명과 우승자의 황금 트로피를 표시합니다. 트로피 상승·축하 음악·색종이 연출 후 9초가 지나면 꼴찌가 커피를 만드는 커피차 장면으로 이어집니다. 커피를 모두 나눈 뒤 반복 하이라이트가 재생됩니다. 시상식 일시정지·건너뛰기·다시 보기를 지원합니다. 2명 참가·휴대폰 화면도 검사했습니다. 배경에는 행사명과 52G·2026 해커톤 반복 격자 스폰서 월을 배치했습니다.</div><p>모션 감소 설정에서는 트로피를 올린 정지 자세를 보여 줍니다. 아래 골든 기준은 모션 감소 상태입니다. 상승 애니메이션과 자동 전환은 별도 실제 시간 브라우저 검사로 확인했습니다.</p>${featureGallery('winner-podium')}`));
await write('reports/coffee/index.html',page('달다 달아 · 커피차',`<div class="note">실제 꼴찌가 커피차의 바리스타를 맡고, 나머지 참가자는 웃는 표정으로 줄을 섭니다. 커피 따르기 → 잔 전달 → 손님의 잔 받기를 로그의 최종 순서와 장면 시각으로 계산합니다. 모든 손님에게 커피를 나눈 뒤 반복 하이라이트로 전환합니다.</div><p>브라우저 검증: 멘트가 커피차 진입 시 한 번 호출됨, 실제 꼴찌/손님 ID 배정, 8명 참여 시 손님 7명, 따르기·잔 전달, 일시정지·재개·다시 보기·건너뛰기·자동 하이라이트 전환, 휴대폰 가로 넘침 없음. 단위 검사는 1–7명 손님에게 전환 이전에 모두 잔이 전달되는지 확인합니다.</p><p>모션 감소 환경의 골든 기준은 커피를 따르는 정지 자세입니다. 실제 동작은 모션 감소를 해제한 브라우저 시나리오에서 검사합니다.</p>${featureGallery('coffee-truck')}`));
const posters=await read('reports/sponsors/artwork.json')||[];
const posterSources={charging:'https://www.gschargev.co.kr/home',group:'https://www.gs.co.kr/ko/about-gs',refining:'https://www.gs.co.kr/ko/about-gs/subsidiaries',retail:'https://www.gs.co.kr/ko/about-gs/subsidiaries',construction:'https://www.gs.co.kr/ko/about-gs/subsidiaries',energy:'https://www.gsenergy.co.kr/',generation:'https://www.gseps.com/html/overview/history.php?mid=7',trading:'https://www.gsgcorp.com/Kr/Business/Outline.aspx'};
const facilities=await read('reports/sponsors/buildings.json')||[];
const facilityNames={charging:'충전 구획 · 충전기 · 전기차',group:'복합 사옥 · 유리 로비 · 외벽 전광판',refining:'캐노피 · 주유기 · 호스 · 매장',retail:'GS25 진열창 · 입구 · 테라스',construction:'층별 골조 · 타워 크레인 · 현장 가림막',energy:'태양광 · 전력 저장 캐비닛 · 탱크',generation:'LNG 탱크 · 발전 스택 · 터빈',trading:'물류창고 · 컨테이너 · 배송 트럭'};
const buildingGallery=`<div class="gallery">${facilities.map(f=>`<figure><a href="buildings/${esc(f.business)}.png"><img src="buildings/${esc(f.business)}.png" alt="${esc(f.name)} ${esc(facilityNames[f.business])}"></a><figcaption><strong>${esc(f.name)}</strong> · ${esc(facilityNames[f.business])}</figcaption></figure>`).join('')}</div>`;
const posterGallery=`<div class="gallery">${posters.map(p=>`<figure><a href="artwork/${esc(p.business)}.png"><img src="artwork/${esc(p.business)}.png" alt="${esc(p.name)} ${esc(p.caption)}"></a><figcaption><strong>${esc(p.name)}</strong> · ${esc(p.caption)} · <a href="${esc(posterSources[p.business])}">공식 사업 소개</a></figcaption></figure>`).join('')}</div>`;
await write('reports/sponsors/index.html',page('GS 건물 광고 · 52G 스폰서 월',`<div class="note">시상대 배경은 참고 사진의 행사명 상단·반복 격자 스폰서 월을 본떠 구현했습니다. 52G · 2026 해커톤을 청록색과 흰색 타일에 반복하고, 흰색 시상대와 붉은 바닥으로 연결합니다. 트로피 상승과 2·1·3 순위를 유지합니다.</div><p><a href="https://www.yna.co.kr/view/AKR20130419105200007">시상대 구성을 참고한 연합뉴스 사진</a>. 사진 파일을 게임에 포함하지 않고 코드와 CanvasTexture로 직접 그립니다. 명칭 참고: <a href="https://www.gs.co.kr/ko/about-gs/subsidiaries">GS그룹 공식 계열사 소개</a> · <a href="https://www.52g.gs/">52g 공식 사이트</a>. 2026 해커톤은 사용자가 요청한 행사 문구입니다.</p><h2>회사별 사업을 표현한 광고 8종</h2><p>충전기·전기차, 산업 연결, 주유기·유류·화학, 편의점·쇼핑백, 건물·크레인, 전력망·저장·수소, LNG·바이오매스 발전, 지구본·무역선으로 각각 구분합니다. 아래 원본은 게임의 실제 Canvas 생성 함수를 Chrome에서 실행해 저장했습니다. 영어 헤더는 이 게임용 디자인 문구이며 공식 캠페인 슬로건을 복제하지 않습니다.</p>${posterGallery}<h2>회사 사업에 맞춘 3D 시설</h2><p>게임에서 사용하는 실제 건물 생성 함수를 WebGL로 렌더링했습니다. 아래 개별 모델은 도시 재료를 사용하며, 6개 맵에서는 외장·지붕·높이·부대 요소가 테마에 맞게 바뀝니다. 정적 메시를 재질별로 병합하고, 도로와 시설 부지·다른 시설의 간격을 확보합니다. <a href="buildings.json">시설별 구성과 치수</a>.</p>${buildingGallery}<h2>배경 구조물에 붙인 회사 광고</h2><table><tr><th>맵</th><th>광고가 부착되는 곳</th></tr><tr><td>로스터리</td><td>따뜻한 외장과 회사별 저층 시설의 간판</td></tr><tr><td>해변</td><td>밝은 모래색 외장과 회사별 시설</td></tr><tr><td>숲</td><td>목재색 외장·울타리와 회사별 시설</td></tr><tr><td>도시</td><td>고층 사옥·크레인·사업별 시설의 외벽 전광판</td></tr><tr><td>설원</td><td>흰 지붕·눈더미·목재색 외장의 회사별 시설</td></tr><tr><td>화산</td><td>산업용 외장과 사업별 설비의 간판</td></tr></table><p>각 맵에 GS차지비·GS그룹·GS칼텍스·GS리테일·GS건설·GS에너지·GS EPS·GS글로벌 8종을 배치합니다. 건물은 도로를 향하며 노면과 다른 광고 건물에서 이격합니다. 해당 부지에 일반 건물·나무가 겹쳐 생성되지 않습니다. 커피차의 GS차지비·GS그룹 배경 보드에도 해당 회사의 새 광고 그림을 적용합니다.</p><h2>검증</h2><p>6개 맵 각각에서 더 촘촘하게 표본화한 도로와 건물 부지의 간격, 광고 건물 상호 간격, 도로를 향하는 방향을 단위 검사했습니다. 브라우저에서 8개 회사·각 맵의 구조물 종류·콘솔 오류를 확인하고, 6개 주행 장면과 데스크톱·휴대폰 시상대 화면을 골든으로 비교합니다. 골든은 이 구현의 회귀 검사이며 참고 사진과의 픽셀 일치도를 측정하지 않습니다.</p>${featureGallery('winner-podium')}${featureGallery('sponsor-scenery')}`));
if(process.argv.includes('--publish')){await fs.mkdir('dist/reports',{recursive:true});await fs.cp('reports','dist/reports',{recursive:true});}
console.log(`Reports written: reports/index.html (${allPassed?'all automated checks passed':'pending / failed checks remain'})`);
