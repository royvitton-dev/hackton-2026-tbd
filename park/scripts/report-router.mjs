import {readFile,writeFile,mkdir,copyFile} from 'node:fs/promises';
const root=new URL('../reports/router/',import.meta.url);
await mkdir(root,{recursive:true});
const read=async name=>{try{return JSON.parse(await readFile(new URL(name,root),'utf8'));}catch{return null;}};
const [unit,coverage,browser,development]=await Promise.all([read('unit.json'),read('coverage/coverage-summary.json'),read('e2e/results.json'),read('development/results.json')]);
const escape=value=>String(value??'미실행').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const totals=coverage?.total||{};
const metrics=Object.entries(totals).filter(([name])=>['lines','statements','branches','functions'].includes(name));
const images=['router-directory','router-directory-mobile','router-mobility'];
await mkdir(new URL('screenshots/',root),{recursive:true});
for(const name of images)await copyFile(new URL(`../tests/router-e2e/golden/${name}.png`,import.meta.url),new URL(`screenshots/${name}.png`,root));
const html=`<!doctype html><html lang="ko"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>TBD 통합 서버 검증</title><style>body{margin:40px auto;max-width:1000px;padding:0 24px;color:#234b41;background:#f5f2eb;font:16px system-ui;line-height:1.8}a{color:#226b56}section{background:#fff;padding:24px;border-radius:12px;margin:20px 0}table{width:100%;text-align:left;border-collapse:collapse}td,th{padding:10px;border-bottom:1px solid #ddd}.gallery{display:flex;gap:16px;flex-wrap:wrap}.gallery a{width:290px}.gallery img{width:100%;border:1px solid #ddd}code{background:#e6e9df}</style><a href="/projects/">← 모든 프로젝트</a><h1>하나의 서버, 경로별 검증.</h1><p>${escape(new Date().toLocaleString('ko-KR'))} 생성</p><section><h2>실행 결과</h2><p>단위 테스트: <b>${escape(unit?.numPassedTests)} 통과</b> / ${escape(unit?.numFailedTests)} 실패</p><p>브라우저: <b>${escape(browser?.stats?.expected)} 통과</b> / ${escape(browser?.stats?.unexpected)} 실패 / ${escape(browser?.stats?.skipped)} 건너뜀</p><a href="e2e/">브라우저·골든 HTML 상세</a>${development?` · <a href="development/">개발 모드 검증 (${escape(development.stats.expected)} 통과)</a>`:''} · <a href="coverage/">커버리지 HTML</a> · <a href="unit.json">단위 테스트 원본</a></section><section><h2>커버리지</h2><p>측정 대상: <code>park/server/routes.mjs</code>, <code>park/server/trading-proxy.mjs</code>. 전체 앱의 커버리지가 아닙니다.</p><table><tr><th>지표</th><th>커버리지</th><th>실행 / 전체</th></tr>${metrics.map(([name,value])=>`<tr><td>${escape(name)}</td><td>${escape(value.pct)}%</td><td>${escape(value.covered)} / ${escape(value.total)}</td></tr>`).join('')}</table></section><section><h2>검증 범위</h2><p>10개 프로젝트의 경로 접근, 새로고침, 실제 게임·영상·도면·3D 차량 동작, 같은 포트의 HTTP와 WebSocket, PC·모바일 골든 비교를 확인합니다. 프록시는 모의 백엔드의 응답·장애·타임아웃·WebSocket 전달을 검사합니다. 실제 거래 엔진과 네이티브 음성 인식의 실행 성공을 뜻하지 않습니다.</p></section><section><h2>골든 기준</h2><div class="gallery">${images.map(name=>`<a href="screenshots/${name}.png"><img src="screenshots/${name}.png" alt="${name}">${name}</a>`).join('')}</div></section><p><code>npm run server:test</code> → <code>npm run server:test:e2e</code> → <code>npm run server:reports</code></p></html>`;
await writeFile(new URL('index.html',root),html);
console.log('HTML report: http://localhost:5190/reports/router/');
